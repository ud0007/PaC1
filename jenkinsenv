pipeline {
  agent any
  environment {
    subject = "Jenkins PaC"
    batchname = "Devops"
  }
  stages {
    stage ('my first stage') {
      steps {
        script {
          print "My subject is ${env.subject}"
          print "My batchname is ${env.batchname}"
        }
      }
    }
  }
}
